# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Build output
.svelte-kit
build
dist
.turbo
coverage
playwright-report
test-results

# Static assets, docs
apps/workbench/static
docs/design
docs/design-day2
CLAUDE.md

# Editor/agent config, kept out of the repo by a global ignore but still on disk
.claude

# Generated by `vitest -u` (see trace-fixture.test.ts). Prettier and the
# snapshot writer disagree about formatting, and the snapshot writer has to win
# — otherwise formatting the file breaks the test that regenerates it.
packages/packs/starter/src/fixtures/trace.say-hello.v1.json

# Generated eval artefacts: the scorecard and baselines are written by
# `npm run evals`, so formatting them here would fight the generator and make
# every run show a spurious diff.
packages/evals/baselines

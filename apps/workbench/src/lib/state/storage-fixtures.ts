/**
 * Moved to `@craftabot/core/testing` with the contract suite they serve (WP36
 * stage A). Re-exported for one release.
 */
export {
	makeAgent,
	makeAgentV1,
	makeEvent,
	makeGroupRun,
	makeRun,
	makeRunSummary,
	makeSpec,
	makeSpecV1,
	uuid
} from '@craftabot/core/testing';

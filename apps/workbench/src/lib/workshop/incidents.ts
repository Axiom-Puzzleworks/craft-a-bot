/**
 * Moved to `@craftabot/governance/reports` (WP36 stage B). Re-exported for one release.
 */
export {
	incidentsFrom,
	type Incident,
	type IncidentFinding,
	type IncidentKind
} from '@craftabot/governance/reports';

# CLAUDE.md — Craft A Bot

## What this is

An LLM & agent simulator styled as a 1970s/80s construction toy. Users snap bricks (LLM, Memory, Tools, Sense, Actions, Safety) onto a workbench, slot in a Goal Card, and watch their agent act in a simulated Playroom with every prompt, decision and action visible in a trace.

Two purposes, in priority order: (1) an accessible training ground for agentic-AI concepts; (2) a proving ground for automated AI governance and guardrails, whose components will eventually be exported for real-world use.

Stack: TypeScript + Svelte 5 (runes) + SvelteKit static. Monorepo (npm workspaces + Turborepo). Local-first, no backend. OpenAI out of the box (BYO key).

**Where we are (2026-08-21):** V1.0 and Day 2 Phases A–F are fully shipped — every WP `18-…` names, WP0 through WP34, is done. The roadmap's forward plan has nothing left in it. **Amended 2026-09-01:** Phase G is now fully closed — **WP35, the Armour Brick** (`25-ARMOUR-BRICK.md`, `18-…` §3 Phase G), all five stages built and the live Google Cloud checkpoint (a real Model Armor verdict, the CORS go/no-go on direct browser calls) confirmed clear. **Amended 2026-09-02:** a fresh planning pass produced `26-TARGET-DESIGN-V3.md` (the target design) and `27-DAY3-ROADMAP.md` (Phases H–L, WP36–WP52); the forward plan is `27-…`, and nothing in it has been built yet. **Amended 2026-09-05:** `27-…` is exhausted (WP36–WP52 done); `41-TARGET-DESIGN-V4.md` is now the target design and `42-DAY4-ROADMAP.md` (Phases M–Q, WP53–WP73) the forward plan, nothing in it built yet. Full history — what each WP built, in what stages, with what divergences — lives in `18-…` §7 (the dated close-out log) and in each WP's own dated amendment inside the doc it touched; this file doesn't restate it. **Amended 2026-09-10:** `42-…` is exhausted (WP53–WP73 done); `64-TARGET-DESIGN-V5.md` is now the target design and `65-DAY5-ROADMAP.md` (Phases R–W, WP74–WP93) the forward plan. Day 5 is built on one branch, `day5`, with no pull request until the whole sprint is reviewed; `main` stays where it is.

## Source of truth: `docs/design-day2/`

**`docs/design/` is superseded. Do not read it or cite it.** Everything lives in `docs/design-day2/`, which contains the Day 1 baseline (00–11, carried forward with status banners) plus the Day 2 documents (12–19). Paths below are relative to that folder.

| Doc | Read when |
|---|---|
| `README.md` | First, if you want the full Day 2 map |
| `00-PROJECT-OVERVIEW.md` | Vision, principles, canonical glossary |
| `01-ARCHITECTURE.md` | Structure, packs, cross-package boundaries |
| `02-AGENT-MODEL.md` | Engine, bricks, loop, world, **event catalogue (§7)** |
| `03-UI-UX-DESIGN.md` | Any Kit screen or interaction |
| `04-VISUAL-DESIGN-LANGUAGE.md` | Styling, tokens, assets |
| `05-TECH-STACK.md` | Tooling, libraries, project layout |
| `06-LLM-PROVIDERS.md` | Providers, cartridges, keys |
| `07-DATA-MODEL-PERSISTENCE.md` | Storage, kit files, traces |
| `08-GOVERNANCE-GUARDRAILS.md` | Guardrails, approval, trace guarantees |
| `10-CODING-STANDARDS.md` | Always — conventions and definition of done |
| `11-VISUAL-ASSET-MANIFEST.md` | Assets, placeholders, art swap-in |
| `12-CURRENT-STATE-ASSESSMENT.md` | Root causes C1–C8, defect register D1–D17/T1–T5 — check before planning new work |
| `13-BRICK-TEST-STRATEGY.md` | Writing tests; the L0–L5 pyramid, per-brick charters, eval harness, conformance kit |
| `14-BRICK-REFERENCE-DESIGNS.md` | Touching any brick or engine shape — the open brick contract, evolutions E1–E12, v2 schemas |
| `15-UIUX-DUAL-MODE.md` | Anything spanning Kit and Workshop |
| `16-TEACHING-AID-UIUX-IMPROVEMENTS.md` | Kit UX work — P0/P1/P2 with acceptance criteria |
| `17-PRO-MODE-UI-DESIGN.md` + `mockups/pro-mode-mockup.html` | Workshop (professional mode) work |
| `18-DAY2-ROADMAP.md` | The full WP0–WP34 record and close-out log (§7) — read before proposing new phases |
| `19-AI-SAFETY-GOVERNANCE-REFERENCE.md` | Governance features — the 38-control catalogue, dip in as needed |
| `25-ARMOUR-BRICK.md` | WP35 (Phase G) — the Armour Brick: Model Armor as a hosted, Workshop-only safety brick; the credential/network seams, `guardrail.external`, decisions D1–D5, stages A–E |
| `26-TARGET-DESIGN-V3.md` | **The current target design** (Day 3) — the safety proving ground: gap register G1–G20, the vendor-neutral guardrail-service shell, evaluators, scenarios, campaigns, egress, telemetry sinks, the headless harness, socket capacity; read before any Phase H–L work |
| `27-DAY3-ROADMAP.md` | **The current forward plan** — Phases H–L, WP36–WP52, each mapped to a `26-…` section and the gaps it retires; §8 is the session-sized to-do |
| `41-TARGET-DESIGN-V4.md` | **The current target design** (Day 4) — the Retail Financial Services Playground: the foundations assessment and decisions D2–D4 (§2.1), gap register G21–G42, desk worlds and `@craftabot/desk`, truth, counterparts, service lines, the synthetic bank and three desks, cohorts and parity, the assurance pack, principal, fork, the harness at scale, the evidence store, the Control Room, editions, the Boundary map; read before any Phase M–Q work |
| `42-DAY4-ROADMAP.md` | **The current forward plan** — Phases M–Q, WP53–WP73, each mapped to a `41-…` section and the gaps it retires; Phase M (foundations) first; §8 is the session-sized to-do |
| `47-SERVICE-LINES.md` | WP58 (Phase M) — service lines and cassettes: `ServiceLine` as registered content with the registry synthesising a tool per operation under the Connector's ids, `simulate`/`cassette`/`live`, `ToolResult.errorKind` and `error.kind: 'cassette-miss'`, the `craftabot-cassette` file, `craftabot record` under the line's own egress, `checkServiceLine`, the Open-Meteo line with its live checkpoint and shipped cassette, the Connector's `serviceLines` picker; stages A–C |
| `46-COUNTERPARTS.md` | WP55 (Phase M) — counterparts: `CounterpartScript` and `advanceCounterpart` in `@craftabot/desk`, the scripted visitor inside the desk (pressure and tags on the transcript line, the `counterpart` injection kind), `AgentHandle.role` and `group.started.memberRoles`, the desk's two-seat `forAgent` (`brief`, `say`, `hang-up`), the `scripted-counterpart` brain and tier, the two-seat golden, `craftabot run --counterpart`, the Kit's duo seating the visitor, the Run Lab's group Boundary; stages A–C |
| `64-TARGET-DESIGN-V5.md` | **The current target design** (Day 5) — the bank in motion: the foundations assessment and decisions D5–D9 (§2.1), gap register G43–G58, tenets 19–26, the calibrated population with its cited calibration table and the synthetic performance label, workflows with stages and executors and the five reference configurations by autonomy level, the context ladder and the ontology, `@craftabot/metrics` (fairness, drift, human load, intervals, the validation suite), the clock and the Monitor, the Worker host, books and the lending knobs, the three lenses, experiments and the Control Effectiveness Register, publishing on axiom-verity.com; read before any Phase R–W work |
| `65-DAY5-ROADMAP.md` | **The current forward plan** — Phases R–W, WP74–WP93, each mapped to a `64-…` section and the gaps it retires; Phase R (foundations for evidence) first; §5 the build discipline; §8 is the session-sized to-do |
| `62-THE-TAIL.md` + `63-ART-COMMISSION-BRIEF-WAVE-2.md` | WP73 (Phase Q, the tail) — the wave 2 brief (fourteen files) with every seam built on a placeholder that passes the contract (`lib/assets/instruments.ts`, `Roundel.svelte`, `Strip.icon`; `lib/assets/finishes.ts` as `--cab-finish-graph`/`--cab-finish-metal` on the Workshop's root, every instrument's rule the fallback; `brand/box-playground` on the shelf; `wave2.test.ts`); `examples/python-reader/` (`read_bundle.py` validating against `docs/schemas/` and recomputing the three digests, the say-hello bundle fixture held byte-equal, `npm run example:python` skipping itself without Python, in CI); `smoke:azure` beside `smoke:geap`, both checkpoints attempted and recorded pending in `30-…`/`39-…`; the eleven findings in §2; stages A–C |
| `61-LAST-DECKS.md` | WP72 (Phase P) — the last decks: `provider-fault { atTick, fault, count? }` as the fifth injection kind delivered by the session (`SessionOptions.providerFaults`, `error` + `provider.retried` per faulted call, the run going on; `splitInjections`, `checkDesk` refusing it); the complaints desk `fs-advice/the-complaints-desk` (`purpose: 'complaints'`, DISP's timescales as ticks in truth, `offer-redress` irreversible and gated, three evaluators, `campaigns/fs-complaints-baseline.json`, `/workshop/playground/complaints`); the operational incident on every desk (*Fallback* and *told plainly* in `fs-bank`, an incident card and scenario per desk, the gates); the DISP and SS1/21 rows resolved; `19-…` #5 adopted; the eight findings in §2; stages A–C |
| `60-CONTROL-ROOM-V2.md` | WP71 (Phase P) — the Workshop's screens on the Control Room: every screen on WP57's instruments (`Strip`/`Readout`, `Lamp` through `lib/control-room/outcome.ts`, `Matrix`, `Tape`, `Meter`), Explain and the Chain beside the Boundary, `rampStep` retired; `workshop-visual.spec.ts` over every Workshop route with the clock and locale pinned, baselines per platform and CI's `visual` job; `a11y-workshop.spec.ts`; the eight findings in §2; stages A–B |
| `59-EDITIONS.md` | WP69 (Phase P) — editions and the site (decision D3): `CAB_EDITION` at build time selecting `simulator` · `workshop` · `playground` · `full` (`lib/edition-id.ts`, `lib/edition.ts`, one pack module per box aliased as `$edition-packs`), `paths.base` and the folder per edition, the route guard and `NotInThisBox`, `routePath`, the shelf's *In the Playground*, the Settings door as a link, the import naming the section, `npm run build:editions` with a budget per edition, `scripts/serve-site.mjs`, `playwright.editions.config.ts` and the three smoke specs, CI's `editions` job, `docs/publishing.md`; the nine findings in §2; stages A–C |
| `58-EVIDENCE-STORE.md` | WP70 (Phase P) — the shared evidence store, a sync target for artefacts only (decision D1; never a key, never the source of truth, never required): `EvidenceStore`/`EvidenceItem`/`EvidenceReceipt` in `core` with one digest rule and `PackManifest.evidenceStores`, `@craftabot/evidence` (`evidence/memory`, `evidence/supabase` over PostgREST with the workspace token as a `bearer-token` credential), `checkEvidenceStore`, `craftabot evidence push|pull`, `/workshop/evidence`, the Audit Centre's and Campaigns' push, `docs/evidence-setup.md`; the eight findings in §2; stages A–C; the live checkpoint taken against a real project |
| `57-HARNESS-AT-SCALE.md` | WP68 (Phase P) — the harness at scale: `RunCampaignOptions.execute(cell, run)`/`concurrency`/`shard` with `prepareCampaign`, `runCampaignCell`, `shardCells` and results placed by ordinal (`CampaignCell.ordinal`, `CampaignReport.shard`); the worker pool over the harness's built `campaign-worker.js` (`--jobs`), `--shard i/n`, `--seeds a-b`, `craftabot merge`, `--resume` over `<out>/cells.jsonl`, `index.jsonl` on the file store and `craftabot index --rebuild`, `CreateSessionGroupDeps` untouched; the file sink's JSONL imported as a run still going (the harness half of live trailing); the CI scale check; the seven findings in §2; stages A–C |
| `56-LIVE-COUNTERPARTS.md` | WP64 (Phase N, built in Phase O) — live counterparts and the Compliance Watchbot: `campaign.counterpart` (`scripted` \| `live`, one per campaign), `guards[].group`, the two-seat cell (`runDuoCell`) on `CreateSessionGroupDeps.world`, the cell's and report's `counterpart` and the `no-regression` gate refusing the other tier, `counterpart-seat.ts` in `evals` over `DeskWorldInstance.seatedCounterpart`, `createEvaluatorCircuitBreaker`/`createComplianceWatchbot` in `pack-monitor`, the `compliance-watchbot` guard on every desk baseline, `craftabot run --counterpart --stack`, `smoke:counterpart`, `BrainChoice.live` and the duo's live Ollama seat, `lib/workshop/stacks.ts` and the Spec Lab's named stacks; the twelve findings in §2; stages A–C |
| `55-PRINCIPAL.md` | WP65 (Phase O) — principal, delegation and attestation on the trace: `Principal`/`Attestation` in core, `run.started.principal?`, `approval.resolved.by?`, `action.performed.attestation?` (`guardrailsPassed` from the `pre-act` chain), `group.started.principal?` with members acting `onBehalfOf` — all written only when the host names one; `SessionOptions.principal`, `resolveApproval(approved, by?)`; `RunSummary.principal`, the Boundary's `human.principal`, the OTel `gen_ai.agent.id`/`craft_a_bot.principal.*`, the assurance pack's *principal* and *validated by* recorded (`principalsOver`); `principalFromEnv`/`--principal`/`CRAFTABOT_PRINCIPAL` in the harness; `browserPrincipal` (`cab.principal.v1`) and Settings' *Your name, on the trace*; the Run Lab's *started by* chip and `Chain`; D21 (the digest over parsed events); stages A–C |
| `54-FORK-EXPLAIN.md` | WP66 (Phase O) — counterfactual replay and explain-this-decision: `WorldInstance.restore?` (the desk and the Playroom), `forkSession` in core (`deps.fork`; the world restored or rebuilt from its recorded calls, the memory window, notebook, feedback, usage and history refolded from the trace), `run.started.forkedFrom`/`RunRecord.forkedFrom`, `createMockProvider.startAt`, `craftabot fork` with `divergence`/`acts`, `decisionExplanation`/`explanationsForTicks` in `governance/reports` with `related`, the assurance pack's incidents quoting them, the Run Lab's Explain panel and "Fork from this tick", Compare's `from=`; the six contract findings in §2; stages A–C |
| `53-ASSURANCE-PACK.md` | WP67 (Phase O) — the assurance pack and the control map: `ControlMap` in `core` on `PackManifest.controlMaps`, `checkControlMap`, `genericControlMap` in `governance/reports` and the bank's and desks' maps (every row `unreviewed`, two `pending`), `assurancePackFor`/`assurancePackFromStorage` with a digest and the two renderers, `craftabot assurance`, `/workshop/assurance`, the Audit Centre download; stages A–C |
| `52-FS-LENDING.md` | WP63 (Phase N) — the Lending Desk, written against the contracts alone: the four contract changes it needed (`prompt-contains` in `core`/`governance`, `ledger.loans`/`appeals` in `fs-bank`, `reasonsUsed` in `governance/reports`, the campaign cell's seed reaching the world in `starter`/`evals`) and one gap recorded, the bank's lending rule in truth with reason codes tied to evidence, the matched pair, nine cards and sixteen scenarios, five cards (*Cohort-blind* on the new leaf), four deterministic evaluators (`explanation-faithful` on `reasonsUsed`) and the understanding rubric, `campaigns/fs-lending-baseline.json` with the first matched parity gate, its red run and the planted skew, `/workshop/playground/lending`; stages A–D |
| `51-FS-FRAUD.md` | WP62 (Phase N) — the Fraud Desk, written against the contracts alone: the two desk-runtime changes it needed (`ctx.open`, predicates' `test(state, truth)`) and two gaps recorded, hand-built alert queues with labels and reasons in truth, `verify-caller`, eleven cards and seventeen scenarios, the five cards, eight deterministic evaluators (`alert-decision` with `labelSemantics: confusion`) and two rubrics, `campaigns/fs-fraud-baseline.json` with derived-metric and parity gates and its red run, `/workshop/playground/fraud`; stages A–D |
| `50-DOMAIN-METRICS.md` | WP61 (Phase N) — domain metrics, cohorts, the campaign report v2: `Evaluator.labelSemantics?`, `WorldDefinition.metrics?` and the desk's five per-case metrics, `DeskTruth.cohort`, the cell's `labels`/`caseMetrics`/`cohort`, the widened `metricNameSchema`, `where.cohort`, the `derived-metric`/`label-rate`/`parity` gates, `summariseCampaign` and the report v2 with its v1 reader, the four renderings, `telemetrySeries` extras and the `series` drift flag; stages A–C |
| `49-FS-ADVICE.md` | WP60 (Phase N) — the Advice Desk: `@craftabot/pack-fs-advice` (content only) over the bank, hidden answers earned by `ask-suitability-question`, the suitability rule in truth, six tiered actions, sixteen layouts with a persona each, sixteen cards and thirty tagged scenarios, the seven policy cards, nine deterministic and four rubric evaluators, `campaigns/fs-advice-baseline.json` and its red run in CI, the `PlanSource` seam (`chainPlans`, `harness/src/plans.ts`), `/workshop/playground/advice`; stages A–D |
| `48-FS-BANK.md` | WP59 (Phase M) — the synthetic bank: `@craftabot/pack-fs-bank` (content only, no runtime), `bankCase(seed)` and `bankRecords` (revealed, hidden, truth; classification on every record), the nine lines answering from `BankExtra` on `worldState.extra` with mutations returned as ledger data, the tenet-13 purpose property, the ten personas, `OBLIGATION_TAGS`, the control-map rows as data, `docs/playground.md`, the shelf box, `/workshop/playground`; stages A–C |
| `45-TRUTH-SYNTHETIC.md` | WP54 (Phase M) — truth and the synthetic sweep: `WorldInstance.truth?()` and `run.finished.truth` (the only event that carries it), `EvaluationInput.truth`, `Evaluator.reads` and `inputReadableBy` on every run path, `checkEvaluator`'s truth checks, `DeskTruth` in `@craftabot/desk` held beside the state, `checkDesk`'s tenet-13 property over 100 seeds, the Run Lab's flap (never the Kit's), the nine synthetic primitives, `checkSynthetic` and the repo-wide sweep, hard rule 9; stages A–C |
| `44-CONTROL-ROOM.md` | WP57 (Phase M) — the Control Room: the token extension, `lib/control-room/dataviz.ts` (the one grammar; teal for magnitude), `components/control-room/*` (Readout, Strip, Lamp, Meter, Tape, Matrix, CaseTable, Transcript, CaseFile, Queue, Boundary), the chart lint rule, `boundaryMapFor` in governance, the Spec Lab and Run Lab Boundary, the `visual` Playwright project; stages A–C |
| `43-DESK-WORLDS.md` | WP53 (Phase M) — desk worlds: `WorldDefinition.view?`, `DeskWorldState` beside `GridWorldState`, `WorldStage`/`DeskView`, the goal-card `audience` gate, the Front Desk in the Workshop pack, `@craftabot/desk` and `createDeskWorld`, the desk golden trace, `checkDesk`; stages A–C |
| `40-DEBTS.md` | WP52 (Phase L) — the debts: the autonomy dial in the Spec Lab, semver ranges evaluated (`satisfiesRange`, `requiresPacks`, `version-mismatch`, `manifest.requires-satisfied`), the Ollama address as a loopback-only Settings field, `ArmourPanel` verified unnecessary, `personas` declaring `openai`; stages A–C |
| `39-HOSTED-EVALUATOR.md` | WP51 (Phase L) — `geap/eval/*`: the Gen AI evaluation service as three hosted evaluators on the `geap` battery, fixtures and an offline stand-in, the `*-suffix` egress label, live paths in the harness, the Workshop and campaigns under a budget; the live checkpoint pending; stages A–C |
| `38-GOVERNANCE-1-0.md` | WP50 (Phase L) — `@craftabot/governance` 1.0.0-rc.1: README, TSDoc audited by a test, `examples/plain-node-agent`, `docs/governance-mapping.md`, the tarball check in CI; stages A–C |
| `37-DRIFT-SAFETY-CASE-RUN-LAB.md` | WP49 (Phase K) — drift: `telemetrySeries`/`driftIn` and the `/telemetry` time axis; the safety case's evaluation evidence and campaign results (`campaignEvidenceFor`, `builds` on the campaign report); the engine's resume, breakpoints as a preference, the live-run bus and the Run Lab trailing it; campaign tiles on the Bench; stages A–C |
| `36-BUNDLE-AND-GROUPS.md` | WP48 (Phase K) — the trace bundle (`buildTraceBundle`/`verifyBundleDigest`, `craftabot-bundle` v1), group observers and the group Watchbot with its circuit breaker, Hearing per seat, episodes in the Audit Centre, the Run Lab's badge over a bundle, `craftabot bundle --group`; stages A–C |
| `35-TELEMETRY.md` | WP47 (Phase K) — telemetry sinks: the `TraceSink` contract in core, `@craftabot/telemetry` (the OTel mapping moved and extended to groups, the batcher, `telemetry/otlp-http`, `telemetry/file` on `/node`), `checkSink`, `/workshop/sinks`, live attach, the Audit Centre's send, `craftabot export`, `run --sink`, a campaign's `sinks`; stages A–C |
| `34-CONTENT-STORE.md` | WP46 (Phase J) — the Workshop content store: `ContentRecord` and the synthetic `local` pack, content on all three stores, the registry's `local/` guard, `requires.localContent` in kit files, save on four Workshop screens, the Kit's gated picker, `--content` and `craftabot content`; stages A–C |
| `33-POLICY-V2-PDP.md` | WP45 (Phase J) — policy-as-code v2 and the external PDP: the six `PredicateExpr` leaves, `describeUnsafePattern`, `GuardrailContext.world`, `WorldActionDefinition.progress` (the `MOVEMENT` wart retired), `pdpRequestFor` and `ScreenRequest.policyInput`, `@craftabot/pack-pdp-opa`, the Studio's leaves; stages A–C, live OPA checkpoint taken |
| `32-SCENARIOS.md` | WP44 (Phase J) — scenarios: `ScenarioDefinition` + `injectionSchema`, `WorldInstance.inject?` and the Playroom's four doors, `ToolContext.worldState`, the four cards as scenarios, `runScenario`, the JSONL corpus importer and scenario packs, campaigns by `scenarioId`, the by-tag table, `craftabot scenarios`, `/workshop/scenarios`; stages A–C |
| `31-EVALUATORS.md` | WP43 (Phase J) — evaluators: the `Evaluator` contract and `EvaluationRecord`, cards as evaluators, the rubric judge pack, campaign gates on verdicts, `/workshop/evaluators`, the Monitor Judge, `craftabot evaluate`, `checkEvaluator`; stages A–D |
| `30-SECOND-VENDORS.md` | WP42 (Phase I) — the second and third guard services: the vendor comparison, `pack-guard-local` (Llama Guard, Prompt Guard over Ollama), `pack-azure-content-safety`, the Guard Rack at `/workshop/guards`, the baseline's stacks; the Azure live checkpoint's status |
| `29-GUARD-SHELL.md` | WP39 (Phase I) — the guard shell: `GuardrailService` contract, `createHostedGuardrails` in governance, `pack-geap` on the shell with its golden trace byte-identical, `workshop/guard`, `checkGuardrailService`; stages A–E |
| `28-CAMPAIGNS.md` | WP38 (Phase H) — campaigns: the campaign file and its gates, the adversary tier, the report and its three renderings, the baseline campaign and CI job, `craftabot campaign`, `/workshop/campaigns`; stages A–E |

`09-ROADMAP.md` is history only (the WP0–WP10 record); `18-…` supersedes its forward plan.

Where a Day 2 doc and its Day 1 counterpart differ, **Day 2 wins** — the banner on each carried-forward file says what changed.

## Workflow

**Phase G is closed — WP35, the Armour Brick, shipped in full** (`25-ARMOUR-BRICK.md`, opened 2026-09-01, closed the same day across stages A–E plus the live Google Cloud checkpoint; `18-…` §7 items 30–32). `18-…`'s forward plan is exhausted (WP0–WP35 all done).

**The fresh planning pass is done (2026-09-02):** `26-TARGET-DESIGN-V3.md` is the target design and `27-DAY3-ROADMAP.md` is the forward plan — Phases H–L, WP36–WP52. **Amended 2026-09-02, later the same day:** WP36 and WP37 are done (`27-…` §8 items 1–6 — the storage contract and the analytic folds live in `core`/`governance`, run summaries retire the N+1 screens, and `@craftabot/harness` runs, bundles and reports headless). **Amended 2026-09-02, later still:** WP38 (campaigns) is done too (`27-…` §8 item 7, `28-CAMPAIGNS.md` §8) — **Phase H is closed.** **Amended 2026-09-02, later still:** WP39 (the guard shell) is done — `29-GUARD-SHELL.md` §8, `27-…` §8 item 8: `GuardrailService` in core, `createHostedGuardrails` in governance, `pack-geap` on the shell with its golden trace byte-identical, `workshop/guard`, `checkGuardrailService`. **Amended 2026-09-02, later still:** WP40 (socket capacity) is done — `27-…` §8 item 9: `SLOT_CAPACITY` (`safety: 4`), the Kit's one-well rule kept with a chip, the Spec Lab's Safety stack. **Amended 2026-09-02, later still:** WP41 (egress and credentials v2) is done — `27-…` §8 item 10: the session's `fetch` guard under `egress: 'declared' | 'none'`, every provider and hosted brick declaring its hosts, `--egress none` in CI, timed vault entries and "Test the guard" on the kind. **Amended 2026-09-02, later still:** WP42 is done — `30-SECOND-VENDORS.md` §7, `27-…` §8 item 11: two more service packs, the Guard Rack, the baseline's stacks; **Phase I is closed** (the Azure live checkpoint remains to be taken by someone with a key). **Amended 2026-09-02, later still:** WP43 (evaluators) is done — `31-EVALUATORS.md` §8, `27-…` §8 item 12. **Amended 2026-09-02, later still:** WP44 (scenarios) is done — `32-SCENARIOS.md` §7 and stage notes, `27-…` §8 item 13. **Amended 2026-09-02, later still:** WP45 (policy v2 + the OPA PDP) is done — `33-POLICY-V2-PDP.md` §7 and stage notes, `27-…` §8 item 14, the live OPA checkpoint taken. (Phase J is WP43–WP46; WP44's note that it had closed was wrong.) **Amended 2026-09-02, later still:** WP46 (the content store) is done — `34-CONTENT-STORE.md` §7 and stage notes, `27-…` §8 item 15; **Phase J is closed.** **Amended 2026-09-02, later still:** WP47 (telemetry sinks) is done — `35-TELEMETRY.md` §7 and stage notes, `27-…` §8 item 16. **Amended 2026-09-02, later still:** WP48 (the bundle and multi-agent completion) is done — `36-BUNDLE-AND-GROUPS.md` §7 and stage notes, `27-…` §8 item 17. (Phase K is WP47–WP49; the note that it had closed was wrong.) **Amended 2026-09-03:** WP49 (drift, the safety case v2, the live Run Lab) is done — `37-DRIFT-SAFETY-CASE-RUN-LAB.md` §7 and stage notes, `27-…` §8 item 18; **Phase K is closed.** **Amended 2026-09-03, later:** WP50 (`@craftabot/governance` 1.0) is done — `38-GOVERNANCE-1-0.md` §7 and stage notes, `27-…` §8 item 19. **Amended 2026-09-03, later:** WP51 (the hosted evaluator) is done — `39-HOSTED-EVALUATOR.md` §7 and stage notes, `27-…` §8 item 20; its live checkpoint is pending a fresh token. **Amended 2026-09-03, later:** WP52 (debts) is done — `40-DEBTS.md` §7 and stage notes, `27-…` §8 item 21; **Phase L is closed, and `27-…`'s forward plan is exhausted.** **Amended 2026-09-05:** the fresh planning pass is done — `41-TARGET-DESIGN-V4.md` is the target design and `42-DAY4-ROADMAP.md` the forward plan (Phases M–Q, WP53–WP73). The docs pass (`42-…` §8 item 0) landed the same day: `00-…` §6's eleven glossary rows, the notes in `00-…` §2, `01-…` §5/§8, `10-…` §8 (hard rule 9's placeholder) and `14-…` §7 (the `core` major bump to `1.0.0` is WP56's). **Amended 2026-09-05, later:** WP56 (foundations hardening) is done — `42-…` §8 items 1–3: the budget per build with per-route sizes, `retries: 1` in CI and the Play routes' saved notice the load-sensitive specs wait on, `@craftabot/core` **1.0.0** with the `guardrails` lane removed (`docs/migrations.md`), `craftabot campaign --matrix` replacing the evals CLI, `docs/schemas/` generated and checked on build; D18 and D19 found and fixed on the way. **Amended 2026-09-05, later:** `43-DESK-WORLDS.md` is written and WP53 stage A is done (`42-…` §8 item 4): the view seam, the union, `WorldStage`/`DeskView`, the `audience` gate, the hand-written Front Desk. Stage B is done too (`42-…` §8 item 5): `@craftabot/desk` and `createDeskWorld`, the Front Desk on it, the desk golden trace, `create(layoutId, { random? })`. **Amended 2026-09-05, later:** WP53 is done — `43-DESK-WORLDS.md` §10–§11 and its three stage notes, `42-…` §8 items 4–6: the view seam and `WorldStage`/`DeskView`, `@craftabot/desk`, the desk golden trace, `checkDesk`. **Amended 2026-09-05, later:** `44-CONTROL-ROOM.md` is written and WP57 is done (`42-…` §8 items 7–9): the tokens, the grammar and the components, `DeskView` on them, the Boundary map in the Spec Lab and the Run Lab, the first screenshots. **Amended 2026-09-05, later:** `45-TRUTH-SYNTHETIC.md` is written and WP54 is done (`42-…` §8 items 10–12): truth on `run.finished` and the evaluator gate, the desk's truth and the tenet-13 property, the Run Lab's flap, the synthetic primitives and the CI sweep, hard rule 9. The bank's and the Advice Desk's notes are renumbered `46-` and `47-`. **Amended 2026-09-05, later:** `46-COUNTERPARTS.md` is written and WP55 is done (`42-…` §8 items 13–15): the scripted visitor inside the desk, the second seat with its role on the trace, the `scripted-counterpart` brain and the two-seat golden, `craftabot run --counterpart`, the Kit's duo seating the visitor, the Run Lab's group Boundary. The bank's and the Advice Desk's notes are now `47-` and `48-`. **Amended 2026-09-05, later:** `47-SERVICE-LINES.md` is written and WP58 is done (`42-…` §8 items 16–18): service lines as registered content with their tools synthesised by the registry, cassettes and `craftabot record`, `checkServiceLine`, the Open-Meteo line with its live checkpoint taken and its cassette shipped, the Connector's picker. The bank's and the Advice Desk's notes are now `48-` and `49-`. **Amended 2026-09-05, later:** the Phase M exit review is done (`42-…` §8 item 19): all six criteria met with evidence named, and D20 closed — the bench re-points the Sense and Actions bricks when a card changes world. **Amended 2026-09-05, later:** `48-FS-BANK.md` is written and WP59 is done (`42-…` §8 items 20–22): the synthetic bank as a content-only pack, the generators from one seed, the records split three ways, the nine lines and the purpose property, the personas, the obligation vocabulary, the control-map rows as data, `docs/playground.md`, the shelf box and `/workshop/playground`. **Phase N is open; next: WP60 (`49-FS-ADVICE.md`, the Advice Desk).** **Amended 2026-09-05, later:** `49-FS-ADVICE.md` is written and WP60 is done (`42-…` §8 items 23–26): the Advice Desk as a content-only pack over the bank, the answers earned by asking, the rule in truth, the four decks and the scripted plans, the `PlanSource` seam in `evals` composed by the harness and the Workshop, the seven cards and thirteen evaluators, the baseline campaign with its red run in CI, the Kit's card behind the Workshop door, the desk's Playground page. **Next: WP61 (domain metrics, cohorts, the campaign report v2).** **Amended 2026-09-05, later:** `50-DOMAIN-METRICS.md` is written and WP61 is done (`42-…` §8 items 27–29): labels with their semantics and the confusion fold, the desk's per-case metrics, the cohort from truth, three gate kinds including parity, the report v2 with its summary and v1 reader, the four renderings, drift over evaluation records and campaign reports. **Next: WP62 (`51-FS-FRAUD.md`, the Fraud Desk).** **Amended 2026-09-05, later:** `51-FS-FRAUD.md` is written and WP62 is done (`42-…` §8 items 30–33): the second desk against the contracts alone — two runtime changes (`ctx.open`, `test(state, truth)`), the queues with truth, the calls, the five cards, the first labelled evaluator and confusion matrix, the first derived-metric and parity gates, the red run in CI. **Next: WP63 (`52-FS-LENDING.md`, the Lending Desk).** **Amended 2026-09-06:** `52-FS-LENDING.md` is written and WP63 is done (`42-…` §8 items 34–37): the third desk against the contracts alone — four contract changes (`prompt-contains`, `ledger.loans`/`appeals`, `reasonsUsed`, the campaign cell's seed reaching the world), the rule in truth, the matched pair, the five cards with *Cohort-blind*, the first matched parity gate, the red run and the planted skew in the pack's tests, the baseline in CI. **Phase N is closed; next: Phase O per `42-…` §6.** **Amended 2026-09-06, later:** `53-ASSURANCE-PACK.md` is written and WP67 is done (`42-…` §8 items 38–40; taken up first in Phase O since it needs only WP63): the control map as a content type with `checkControlMap`, the generic and UK rows registered — every row `unreviewed` until a compliance reader has read it, two `pending` WP72 — the assurance pack folded and rendered as markdown and one HTML file a reviewer opens with no app, `craftabot assurance`, `/workshop/assurance`, the Audit Centre download. **Next: WP64 (live counterparts), WP65 (principal — fills the pack's "validated by") or WP66 (fork and explain — fills its explanations).** **Amended 2026-09-06, later:** WP66 (fork and explain) is done — `54-FORK-EXPLAIN.md` §8 and its stage notes, `42-…` §8 items 41–43: `restore?` and `forkSession` in core, `craftabot fork`, `decisionExplanation`, the Run Lab's Explain and "Fork from this tick", Compare synced from the fork. **Next: WP64 (live counterparts) or WP65 (principal — fills the pack's "validated by").** **Amended 2026-09-06, later:** WP65 (principal) is done — `55-PRINCIPAL.md` §8 and its stage notes, `42-…` §8 items 44–46: the three optional fields and the group's, written only when named, both hosts naming theirs, the summary, the Boundary, the OTel attributes and the assurance pack's two sections filled, the Run Lab's Chain; D21 found and fixed on the way. **Next: WP64 (live counterparts).** **Amended 2026-09-06, later:** WP64 (live counterparts) is done — `56-LIVE-COUNTERPARTS.md` §8 and its stage notes, `42-…` §8 items 47–49: the live seat as a campaign instrument that no report compares with the scripted one, the seated person read from the desk, the Compliance Watchbot on every desk baseline and in the Spec Lab, the harness's `--stack` and `smoke:counterpart`, the Kit's duo seating Ollama live. **Next: WP68 (the harness at scale), per `42-…` §6.** **Amended 2026-09-06, later:** WP68 (the harness at scale) is done — `57-HARNESS-AT-SCALE.md` §8 and its stage notes, `42-…` §8 items 50–52: the `execute` seam, the worker pool, shards and `merge`, `--resume`, the store's index, the sink's JSONL imported as a run still going, the scale check in CI. **Next: WP69 (publishing) or WP70 (the evidence store), per `42-…` §6.** **Amended 2026-09-06, later:** WP70 (the evidence store) is done — `58-EVIDENCE-STORE.md` §8 and its stage notes, `42-…` §8 items 53–55: the contract in `core`, `@craftabot/evidence` with the memory and Supabase stores, `craftabot evidence`, `/workshop/evidence`, `docs/evidence-setup.md`; the live checkpoint taken the same day (`42-…` §8 item 56: the migration applied, the suite green over the real project, a run pushed from the harness and read back by SQL). **Next: WP69 (publishing), WP71 or WP72, per `42-…` §6.** **Amended 2026-09-06, later:** WP69 (editions and the site) is done — `59-EDITIONS.md` §8 and its stage notes, `42-…` §8 items 57–59: editions at build time with a pack module per box, the guard, the shelf and the door per edition, `build:editions` with a budget each, the three smoke specs over the three folders served as a host serves them, CI's `editions` job, `docs/publishing.md`. **Next: WP71 (the Control Room v2) or WP72 (the last decks), per `42-…` §6; WP73 is the tail.** **Amended 2026-09-06, later:** WP71 (the Control Room v2) is done — `60-CONTROL-ROOM-V2.md` §8 and its stage notes, `42-…` §8 items 60–61: every Workshop screen on the system, the visual-regression pass over every Workshop route diffed in CI, axe over every Workshop route. **Next: WP72 (the last decks), per `42-…` §6; WP73 is the tail.** **Amended 2026-09-06, later:** WP72 (the last decks) is done — `61-LAST-DECKS.md` §8 and its stage notes, `42-…` §8 items 62–64: the `provider-fault` seam, the complaints desk with its campaign, the operational incident on every desk, the two control-map rows resolved. **Next: WP73 (art wave 2 and the live checkpoints), the tail of `42-…`.** **Amended 2026-09-07:** WP73 (the tail) is done — `62-THE-TAIL.md` §8 and its stage notes, `63-ART-COMMISSION-BRIEF-WAVE-2.md`, `42-…` §8 items 65–67: the wave 2 seams on placeholders, the Python reader, `smoke:azure`, both live checkpoints recorded pending as one command each. **Phase Q is closed and `42-…`'s forward plan is exhausted — every row of its §3 is ✅.** What remains is outside a work package: the two live checkpoints (a key, a token), the wave 2 commission itself, and `42-…` §9's reading by a practitioner. **Starting new work needs a fresh planning pass first.** **Amended 2026-09-07, later:** a UX pass outside any work package — `docs/manual/UX-AND-GAPS.md` §2a records what was resolved of the thirty findings from the manual's walk of the build (twenty-three resolved, one part, six open as work packages for the next plan). It touched core twice, both additive and documented in place: `guardrail.tripped.cause` (02 §7) and `RunRecord.abandonedAt` (07), plus `PackManifest.campaigns` (28). A re-test the same day added six findings (`UX-AND-GAPS.md` §3), all resolved in a second pass (§3a): the Campaigns screen now hands the runner the Workshop's plan chain (`lib/workshop/plans.ts`), and `core` holds the one definition of a finished run (`isRunFinished`/`effectiveOutcome`, 07). **Amended 2026-09-10:** the fresh planning pass is done — `64-TARGET-DESIGN-V5.md` is the target design and `65-DAY5-ROADMAP.md` the forward plan (Phases R–W, WP74–WP93). The docs pass (`65-…` §8 item 0) landed the same day on the `day5` branch: this chain, `README.md`'s index and status note, `41-…`'s status line, `12-…`'s pointer to `64-…` §2.1. **The Day 5 sprint runs on the `day5` branch only**, one commit per stage or WP, CI on every push (the branch is named in `ci.yml`'s trigger for the sprint's duration), no PR to `main` until the whole sprint is reviewed. Where `65-…` asks for a review by Andrew mid-flight (the sourcing note), the work is marked *awaiting review* and continues. **Amended 2026-09-10, later:** `66-CALIBRATION.md` stage 0 (the sourcing note, `65-…` §8 item 1) is written with every row cited and `review: 'pending'`; **WP77 (the Worker host) is done** — `65-…` §3's row and §8 item 3: `lib/worker/` and the runner store with its queue, the Campaigns screen moved onto it, the byte-identity test, the Worker's chunk counted apart by the budget (`01-…` §8, `28-…`'s dated note). **Next: WP74 stage A (the calibration table), then WP78, WP76, WP75, per `65-…` §8.** **Amended 2026-09-10, later:** WP74 stage A is done — `66-…` §5's stage A note and §6: the calibration table in `core` (`docs/schemas/calibration.schema.json`), `checkCalibration`, `fs-bank`'s two tables (`CALIBRATION` cited and pending review; `DECK_WEIGHTS` the WP59 weights `bankCase` keeps), the generators on rows, the 20,000-customer calibration test. **Next: WP78 (the lending knobs), then WP76 B–C, WP74 B–C, WP75, per `65-…` §8.** **Amended 2026-09-10, later:** WP78 (the lending knobs) is done — `65-…` §3's row and §8 item 5: `LendingPolicy` with `DEFAULT_LENDING_POLICY` byte-identical to the rule as it was, the knobs reaching the desk at `create` (`WorldCreateOptions.config`, the one additive core seam), the cards on knob-reading predicates, `builds[].overrides.knobs` in a campaign and on its report. **Next: `68-METRICS.md` and WP76 B–C, then WP74 B–C, WP75, per `65-…` §8.** **Amended 2026-09-10, later:** WP76 (`@craftabot/metrics` and the validation suite) is done — `68-METRICS.md` and its stage notes, `65-…` §3's row and §8 items 2 and 6: the package, `driftIn` delegating, the suite as data, `docs/metrics.md` generated and checked on build (`npm run metrics:doc`). **Next: WP74 stages B–C (the population, the bank page), then WP75, then the Phase R exit review, per `65-…` §8.** **Amended 2026-09-10, later:** WP74 is done — `66-…`'s four stage notes, `65-…` §3's row and §8 item 7: the population with its lazy transaction stream, per-customer seeds and synchronous digest, the calibration test over 20,000 customers, the bank page's two strips. **Next: `67-PERFORMANCE-AND-BOOKS.md` and WP75, then the Phase R exit review, per `65-…` §8.** **Amended 2026-09-10, later:** WP75 is done — `67-…` and its stage note, `65-…` §3's row and §8 item 8: the hazard and the label, the loan book and `lendingBook`, the alert rule and the alert book, `book.schema.json`. **Next: the Phase R exit review (`65-…` §8 item 9), then Phase S: `69-WORKFLOWS.md` and WP79.** **Amended 2026-09-10, later:** the Phase R exit review is recorded (`65-…` §8 item 9): items 1, 2 and 6 of `64-…` §14 met with the evidence named, the Worker's byte-identity and the knobs' default identity in CI, the sourcing note cited on every row and awaiting Andrew's reading. **Phase R is closed. Next: `69-WORKFLOWS.md`, then WP79 stage B, per `65-…` §8 items 10–11.** **Amended 2026-09-10, later:** WP79 (`@craftabot/workflow`) is done — `69-WORKFLOWS.md` §9's two stage notes, `65-…` §3's row and §8 item 10: the contract and the run record in `core` (`workflow-run.schema.json`), the two `stage.*` events with their OTel spans, the package with its four executors and `fromStage`, the desk golden held byte for byte around the stage events, `craftabot workflow run`. **Next: WP80 (the lending workflow and its five configurations), then `70-…` and WP81, per `65-…` §8 items 11–12.** **Amended 2026-09-11:** WP80 is done — `73-LENDING-WORKFLOW-AND-BOOKS.md`, `65-…` §3's row and §8 item 11: the lending journey as content with its five configurations by autonomy level and the ceilings cited, books as a campaign's `source` through the one runner, the human-load rows by level, `craftabot book run`/`sweep`, the Books and Sweeps panels, the book campaign in CI. **Next: `70-CONTEXT-AND-ONTOLOGY.md` and WP81, then WP82, then the Phase S exit review, per `65-…` §8 items 12–14.** **Amended 2026-09-11, later:** WP81 is done — `70-CONTEXT-AND-ONTOLOGY.md`, `65-…` §3's row and §8 item 12: the context ladder in `core` and the desk runtime with `checkDesk`'s superset property, the bank's ontology and the `graph` line as the tenth, the campaign's `contexts` axis, the level measurable by `data-minimised`. **Next: WP82 (gates and report v3), then the Phase S exit review, per `65-…` §8 items 13–14.** **Amended 2026-09-11, later:** WP82 is done — `74-GATES-AND-REPORT-V3.md`, `65-…` §3's row and §8 item 13: `parity` with a metric and its statistics, the `drift` gate, the report v3 with the v2 reader, the two panes; and the Phase S exit review is recorded (`65-…` §8 item 14) — items 3, 4, 5, 7 and 14 of `64-…` §14 met with the evidence named, the Pipeline view and the per-rung token fold left to Phase U. **Phase S is closed. Next: `71-THE-CLOCK.md`, then WP83, WP84, WP85, per `65-…` §8 item 15.**

Once there's a WP to build (from a new plan, or a defect worth fixing): read the docs it names, **propose a task breakdown before writing code**, then build. One WP per branch (`wp{n}-{slug}`) and PR. Use your judgement inside a WP — the docs fix the destination and the contracts, not every step. Where a doc is silent, decide and note it. Where implementation must diverge from a doc, change the doc in the same PR with a dated note (`> **Amended 2026-08-13:** …`); don't leave the two disagreeing.

## Hard rules (violations are bugs)

1. **Engine/UI separation:** `packages/core`, `packages/governance` and `packages/packs/*` never import Svelte or touch the DOM. All UI lives in `apps/workbench`.
2. **Keys are sacred:** API keys live only in `localStorage` (`cab.keys.v1`), read only by provider packs at call time. Never in kit files, traces, events, logs, errors or URLs. The CI key-leak test stays.
3. **Everything observable:** anything the UI shows about engine behaviour arrives as a typed event on the EventBus. New behaviour ⇒ new or extended event, added to the catalogue in `02-…` §7 in the same PR.
4. **Packs contribute content, not mechanisms.** New slot types or hooks are deliberate `core` changes, never pack workarounds.
5. **Determinism:** the world is fully deterministic; all randomness goes through the `dice` tool and is recorded in the trace.
6. **Design tokens only:** no raw colours outside `tokens.css`; the colour↔concept mapping (`04-…` §2.2) is fixed. (Not yet lint-enforced — review discipline.)
7. **Toy names in UI, real names in code:** UI says "battery", code says `apiKey` (glossary: `00-…` §6). Both vocabularies, never a third.
8. **Repo is future-public:** no secrets or private notes in code, comments or history.
9. **Nothing real, ever** (WP54, `45-TRUTH-SYNTHETIC.md`; `41-…` tenet 15): every person, account, card, document, cassette and corpus row in the repo is synthetic, generated from a seed through the primitives in `@craftabot/desk` (`syntheticName`, `syntheticPan`, …). `checkSynthetic` sweeps every fixture, cassette, scenario, baseline, corpus and campaign file in CI (`desk/src/synthetic-sweep.test.ts`) and refuses the shapes real identifiers have. If it fails, regenerate the fixture — never relax the shape.

## Commands

```bash
npm run dev            # Turborepo dev (core watch + workbench on Vite)
npm run test           # Vitest across all packages (workbench runs with coverage)
npm run e2e            # Playwright (the default project), mock provider — no key needed
npm run e2e:visual     # the screenshot set (WP57, every Workshop route since WP71); baselines committed per platform under e2e/__screenshots__/ — CI's visual job diffs on Linux
npm run check          # svelte-check / tsc across workspaces
npm run lint           # prettier --check + eslint + check
npm run format         # prettier --write
npm run build          # all packages + static app + bundle budget (the full edition)
npm run build:editions # the three sections of the site into apps/workbench/build/<edition>/, each against its own budget (WP69)
npm run serve:site     # serve build/ as a static host would — one SPA fallback per folder
npm run e2e:editions   # the three smoke specs, one per edition, against their folders
npm run preview        # serve the built app
npm run budget         # bundle-size budget only (per build: --limit <bytes>, --app <dir>; prints per-route sizes)
npm run schemas        # regenerate docs/schemas/ from the Zod sources (build runs the --check)
npm run evals          # the scripted matrix through the harness: craftabot campaign --matrix scripted
npm run smoke:openai   # live OpenAI smoke test — explicit env key, never in CI
npm run smoke:geap     # live Model Armor + Gen AI evaluation checkpoint — GEAP_* in the env, never in CI (39-…)
npm run smoke:azure    # live Azure AI Content Safety checkpoint — AZURE_CONTENT_SAFETY_KEY/_ENDPOINT in the env, never in CI (30-…)
npm run example:python # examples/python-reader over its bundle fixture — skips itself with no python3 (WP73)
npm run craftabot -- … # the headless host (WP37/38): packs | run [--counterpart] | record | bundle | report | campaign [--jobs --shard --seeds --resume] | merge | index | fork | assurance | evidence push|pull | workflow run | book run | sweep — see packages/harness/README.md
npm run smoke:harness  # live harness run on OpenAI — CRAFTABOT_CREDENTIAL_OPENAI in the env, never in CI
```

## Testing

Mock provider everywhere; live OpenAI only via `smoke:openai`. `core` ≥90% coverage, world predicates 100%, keyboard-only E2E variants for build interactions. Deterministic tests only — no network, no real clocks, no unseeded randomness. Full DoD: `10-…` §8; the Day 2 test estate to build out is `13-…` §L0–L5.

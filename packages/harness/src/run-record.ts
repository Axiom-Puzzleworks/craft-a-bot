/**
 * The derivation moved to `@craftabot/core` (WP84, `75-THE-MONITOR.md` §6)
 * so the evidence sink can bundle an agent run as `craftabot run` records
 * one; re-exported here for the harness's own callers.
 */
export { runRecordFrom } from '@craftabot/core';

/**
 * Semantic string aliases for pack-content ids ("{packId}/{localId}",
 * 01-ARCHITECTURE.md §4). Kept as plain strings — the registry, not the
 * type system, is what proves an id resolves to real content.
 */
export type ToolId = string;
export type ActionId = string;
export type SenseChannelId = string;
export type CartridgeId = string;
export type GoalCardId = string;
export type WorldId = string;
export type BrickId = string;
export type GuardrailId = string;
export type ConceptTag = string;
export type WorldPredicateId = string;

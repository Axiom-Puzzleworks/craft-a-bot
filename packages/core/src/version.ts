/**
 * The engine's own version, as `package.json` says it — what a pack's
 * `requiresCore` and a kit file's `requires.core` are evaluated against
 * (WP52, `40-DEBTS.md` §4.2). A test holds the two together.
 */
export const CRAFTABOT_CORE_VERSION = '0.0.1';

/**
 * The Workshop pack's testing surface (WP55 stage C), the starter pack's
 * `@craftabot/pack-starter/testing` precedent: the scripted plans a harness
 * or a campaign drives a bot along, so a scripted brain can play a desk
 * card without a key. Never imported by the runtime.
 */
export { planFor, SCRIPTED_OPTIMAL, type Plan, type PlanStep } from '../session/plans.js';

/**
 * Every user-facing line this pack owns. The verdict wording comes from the
 * shell's default strings (`@craftabot/governance`'s `defaultHostedStrings`)
 * — a vendor that has nothing better to say should not say it differently.
 */

export const SERVICE_NAME = 'Azure Content Safety';
export const SERVICE_DESCRIPTION =
	'Azure AI Content Safety: Prompt Shields for prompt attacks, plus hate, self-harm, sexual and violence screening with a severity level.';
